I need to do some high-level planning/strategy/organization. We have tasks in both Linear (accessible via CLI) and Notion (accessible by MCP).

Look at the `Tasks` view of the `To-Dos` table in Notion to specifically see the product related tasks.